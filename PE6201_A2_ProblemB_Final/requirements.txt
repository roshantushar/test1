# PE6201 A2 Problem B (Final) - dependencies
#
# The scripted path (data generation, validation, the full evaluation run,
# D2(c), D3(b), D6, D7) uses ONLY the Python 3 standard library - nothing
# in this file is required to reproduce any of results/scripted/,
# results/parallelism/, results/guardrails/, results/cost/, results/d7/.
#
# The live path (D2(b)'s live comparison, D5(b)'s model battery) also uses
# only the standard library (urllib.request) for the OpenRouter call - see
# src/backends.py's _live_call. No package installs are needed for that
# either; only an OPENROUTER_API_KEY environment variable and
# config.BACKEND = "live".
#
# This file exists to make that explicit rather than leaving it implied.
# If you add a notebook kernel or a plotting library for your own report
# figures, list it below - it is not required by anything under src/ or
# experiments/.
